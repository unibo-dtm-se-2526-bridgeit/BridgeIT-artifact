"""Test package for the domain layer (bridgeit.domain).

Mirrors the structure of bridgeit/domain/ so that each domain module
has a corresponding, easy-to-find test module
(e.g. requirement.py <-> test_requirement.py).
"""
